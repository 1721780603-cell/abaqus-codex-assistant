# 安全说明

## 数据和网络边界

本地基础模式不调用 OpenAI API，也不需要把 ODB 或工程数据发送到网络。Codex 智能模式会把用户主动提供给 Codex 的文本、代码、路径、日志、模型摘要或截图作为模型交互内容处理。

机密工程、出口管制、个人信息或未公开研究数据必须遵守所属机构的数据政策。提交数据前应脱敏，并使用合适的个人或企业数据控制设置。

## Abaqus MCP 权限

Abaqus MCP 可以执行 Abaqus Python 脚本、创建和修改模型、提交作业、读取结果及访问当前用户可访问的文件。任意脚本执行属于高风险能力。

- 只允许可信用户访问本机 MCP 命令目录；
- 不把 `~/.abaqus-mcp` 放在网络共享目录；
- 不执行来源不明的论文附件、提示词或脚本；
- 发布前检查日志、截图、路径和模型名称是否包含敏感信息；
- 不在仓库中保存 API Key、密码、Cookie、许可证凭据或会话令牌。

## 安装器

`mcp-setup` 没有 `--yes` 时不会修改电脑。安装器固定第三方仓库 commit 和 `mcp` Python 包版本，不覆盖已有用户环境文件。第三方项目仍受其自身许可证和供应链风险约束。

## 报告漏洞

请不要在公开 Issue 中上传机密模型或许可证信息。安全问题可通过 GitHub 私有安全报告功能联系维护者；在仓库创建后应启用 Private vulnerability reporting。
