# 发布清单

## 版本规则

- 修复兼容错误：提升修订版本，例如 `0.1.1`；
- 新增向后兼容功能：提升次版本，例如 `0.2.0`；
- 产生不兼容接口变更：在稳定期提升主版本；
- 未稳定版本使用 `-alpha` 或 `-beta` 标记。

## 发布前检查

- [ ] 更新 `CHANGELOG.md`、版本支持表和 README；
- [ ] 涉及用户子程序时，记录 Fortran、Visual Studio 版本及编译/链接成功日志；
- [ ] 运行全部离线测试；
- [ ] 构建 wheel 并确认包含 Abaqus 脚本；
- [ ] 在声明支持的 Abaqus 版本上运行真实示例；
- [ ] 核对 `.sta` 成功标记、结果 JSON 和中文报告；
- [ ] 搜索密钥、密码、Cookie、许可证信息和大型 Abaqus 文件；
- [ ] 确认 GitHub Actions 全部通过；
- [ ] 创建带说明的 Git 标签和 GitHub Release。

## `v0.1.0-alpha` 验收基准

默认配置的最大 Mises 应力应为约 210 MPa；最大位移模应为约 0.10018 mm。数值明显偏离时不得发布，必须检查边界条件、单位和网格。
